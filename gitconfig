[user]
  name = Carl Jackson
  email = carl@avtok.com

[alias]
  ama = commit --amend -CHEAD
  amend = commit --amend
  au = add -u
  br = branch
  co = checkout
  cp = cherry-pick
  cpx = cherry-pick -x
  d = diff
  dc = diff --cached
  l = log
  pop = stash pop
  pp = log --graph --pretty=format:'%C(yellow)%h%Creset %Cgreen%s%Creset - %aN, %ar%Cblue%d%Creset'
  rb = "!f() { git checkout -b $1 origin/master; git config \"branch.$1.merge\" \"refs/heads/$1\"; }; f"
  s = status
  st = stash --include-untracked
  sup = "!git stash && git pull --rebase && git stash pop"
  up = pull --rebase
  wip = !git add -A && git commit -m "wip"

[color]
  diff = auto
  status = auto
  branch = auto
  interactive = auto
  ui = auto
  pager = true

[color "branch"]
  current = yellow reverse
  local = yellow
  remote = green

[color "diff"]
  meta = yellow bold
  frag = magenta bold
  old = red bold
  new = green bold

[color "status"]
  added = yellow
  changed = green
  untracked = cyan

[core]
  pager = less -FRS
  whitespace = fix,trailing-space,space-before-tab,cr-at-eol
  editor = vim
  excludesfile = ~/.gitignore

[apply]
  whitespace = fix
[advice]
  pushNonFastForward = false
  statusHints = false
[diff]
  renames = copies
  mnemonicprefix = true
[branch]
  autosetupmerge = true
[push]
  default = upstream
[rerere]
  enabled = true
[merge]
  stat = true
  conflictstyle = diff3

[include]
  path = ~/.gitoverride
